const Blog = async () => {
    return (
        <main className="w-screen flex min-h-screen flex-col items-center justify-between p-24">
            <p>
                This Page will house "in the news" archive. Will configure a
                links and list widget.
            </p>
        </main>
    );
};

export default Blog;
